# Print Shop POS - System Architecture

## 🏗️ Complete System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                    REACT FRONTEND (Phase 3)                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │
│  │ Counter  │  │  Back    │  │ Manager  │  │  Admin   │  ...   │
│  │   App    │  │  Office  │  │   App    │  │   App    │        │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘        │
└───────────────────────┬─────────────────────────────────────────┘
                        │ HTTPS / JWT
┌───────────────────────▼─────────────────────────────────────────┐
│                  LARAVEL API (Phase 2 ✅)                        │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ ROUTES (api.php) - 50+ endpoints                           │ │
│  │  /api/auth/*  /api/orders/*  /api/service-jobs/*          │ │
│  │  /api/products/*  /api/customers/*  /api/pricing/*        │ │
│  └───────────────────────┬────────────────────────────────────┘ │
│                          │                                       │
│  ┌───────────────────────▼────────────────────────────────────┐ │
│  │ CONTROLLERS (6)                                            │ │
│  │  - Request validation                                      │ │
│  │  - Response formatting                                     │ │
│  │  - Permission checks                                       │ │
│  └───────────────────────┬────────────────────────────────────┘ │
│                          │                                       │
│  ┌───────────────────────▼────────────────────────────────────┐ │
│  │ SERVICES (3) - Business Logic                              │ │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │ │
│  │  │   Pricing   │  │    Order    │  │  ServiceJob │       │ │
│  │  │   Engine    │  │   Service   │  │   Service   │       │ │
│  │  └─────────────┘  └─────────────┘  └─────────────┘       │ │
│  └───────────────────────┬────────────────────────────────────┘ │
│                          │                                       │
│  ┌───────────────────────▼────────────────────────────────────┐ │
│  │ MODELS (14) - Eloquent ORM                                 │ │
│  │  User, Role, Customer, Product, PricingRule, Order,        │ │
│  │  OrderItem, ServiceJob, Payment, Invoice, Quotation, etc.  │ │
│  └───────────────────────┬────────────────────────────────────┘ │
└───────────────────────┬─┴────────────────────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────────────────────┐
│              MySQL DATABASE (Phase 1 ✅)                         │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 17 TABLES:                                                 │ │
│  │  users, roles, customers, products, pricing_rules,         │ │
│  │  orders, order_items, service_jobs, service_status_history,│ │
│  │  quotations, quotation_items, invoices, payments,          │ │
│  │  notifications, audit_logs, + Laravel system tables        │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Core Workflows Implemented

### 1. Order Creation Flow
```
User → OrderController::store()
         ↓
     OrderService::create()
         ↓
     ┌───▼────────────────────┐
     │ Validate customer      │
     │ credit (if applicable) │
     └────┬───────────────────┘
          │
     ┌────▼────────────────────┐
     │ Create Order (DRAFT)    │
     └────┬────────────────────┘
          │
     ┌────▼────────────────────┐
     │ For each item:          │
     │  → PricingEngine        │
     │  → Create OrderItem     │
     └────┬────────────────────┘
          │
     ┌────▼────────────────────┐
     │ Calculate totals        │
     │ (subtotal, tax, total)  │
     └────┬────────────────────┘
          │
     ┌────▼────────────────────┐
     │ Return Order with items │
     └─────────────────────────┘
```

### 2. Pricing Calculation Flow
```
PricingController::calculate()
         ↓
PricingEngine::calculate($product, $params)
         ↓
┌────────▼──────────────────┐
│ Get valid pricing rules   │
│ (active, not expired)     │
└────────┬──────────────────┘
         │
┌────────▼──────────────────┐
│ Sort by priority (DESC)   │
└────────┬──────────────────┘
         │
┌────────▼──────────────────┐
│ Try each rule:            │
│  - Customer-specific      │
│  - Dimension-based        │
│  - Quantity-tier          │
│  - Fixed                  │
└────────┬──────────────────┘
         │
┌────────▼──────────────────┐
│ First match wins          │
│ Calculate line total      │
└────────┬──────────────────┘
         │
    Return result
```

### 3. Service Job Workflow
```
Order status → IN_PRODUCTION
         ↓
ServiceJobService::createFromOrder()
         ↓
┌────────▼────────────────────┐
│ For each service/dimension  │
│ item in order:              │
│  → Create ServiceJob        │
│  → Status: PENDING          │
└────────┬────────────────────┘
         │
┌────────▼────────────────────┐
│ Assign to user              │
│ Status: ASSIGNED            │
└────────┬────────────────────┘
         │
┌────────▼────────────────────┐
│ Production starts           │
│ Status: IN_PROGRESS         │
│ Record started_at timestamp │
└────────┬────────────────────┘
         │
┌────────▼────────────────────┐
│ Quality check               │
│ Status: QA_REVIEW           │
└────────┬────────────────────┘
         │
    ┌────▼─────┐
    │ Pass/Fail│
    └─┬──────┬─┘
      │      │
   Pass│     │Fail
      │     ┌▼──────────────────┐
      │     │ Status: REJECTED  │
      │     │ rework_count++    │
      │     │ Back to ASSIGNED  │
      │     └───────────────────┘
      │
┌─────▼────────────────────────┐
│ Status: COMPLETED            │
│ Record completed_at          │
│ Notify counter staff         │
│ Update order status → READY  │
└──────────────────────────────┘
```

### 4. Authentication Flow
```
POST /api/auth/login
      {email, password}
         ↓
┌────────▼────────────────────┐
│ Validate credentials        │
│ (email exists, password OK) │
└────────┬────────────────────┘
         │
┌────────▼────────────────────┐
│ Load user with role         │
└────────┬────────────────────┘
         │
┌────────▼────────────────────┐
│ Create API token (Sanctum)  │
└────────┬────────────────────┘
         │
      Return:
      {user, token, permissions}
         ↓
Frontend stores token
         ↓
All future requests include:
  Authorization: Bearer TOKEN
```

---

## 🎯 RBAC Permission System

```
┌────────────────────────────────────────────────────────────┐
│                    USER MODEL                               │
├────────────────────────────────────────────────────────────┤
│  hasPermission(resource, action)                           │
│    ↓                                                        │
│  Check role.permissions JSON                               │
│    ↓                                                        │
│  Return true/false                                         │
└────────────────────────────────────────────────────────────┘

Example permissions JSON structure:
{
  "orders": ["create", "read", "update", "cancel"],
  "products": ["read"],
  "service_jobs": ["read", "update", "assign"],
  "customers": ["create", "read", "update"],
  ...
}

ROLES:
┌──────────┬──────────┬───────────┬────────────┬──────────┐
│  Admin   │ Manager  │  Counter  │ BackOffice │ Accounts │
├──────────┼──────────┼───────────┼────────────┼──────────┤
│ Full     │ Approve  │ Orders    │ Jobs       │ Finance  │
│ Access   │ Oversee  │ Customers │ Production │ Reports  │
│          │ Reports  │ Payments  │            │ Invoices │
└──────────┴──────────┴───────────┴────────────┴──────────┘
```

---

## 📊 Data Relationships

```
User ──┬─── createdOrders (Order)
       ├─── approvedOrders (Order)
       ├─── assignedServiceJobs (ServiceJob)
       ├─── role (Role)
       └─── notifications (Notification)

Customer ──┬─── orders (Order)
           ├─── quotations (Quotation)
           ├─── invoices (Invoice)
           └─── payments (Payment)

Order ──┬─── customer (Customer)
        ├─── items (OrderItem)
        ├─── serviceJobs (ServiceJob)
        ├─── payments (Payment)
        └─── invoice (Invoice)

Product ──┬─── pricingRules (PricingRule)
          └─── orderItems (OrderItem)

ServiceJob ──┬─── order (Order)
             ├─── orderItem (OrderItem)
             ├─── assignedUser (User)
             └─── statusHistory (ServiceStatusHistory)
```

---

## 🔐 Security Layers

```
1. Authentication (Sanctum JWT)
   ↓
2. Route Middleware (auth:sanctum)
   ↓
3. Role Check (hasRole())
   ↓
4. Permission Check (hasPermission())
   ↓
5. Business Logic Validation
   ↓
6. Database Constraints
   ↓
7. Audit Logging
```

---

## 📈 Performance Optimizations

```
DATABASE
├─ Indexes on: status, customer_id, created_at, order_number
├─ Foreign keys with ON DELETE actions
├─ Pagination (20-50 per page)
└─ Eager loading (with['customer', 'items'])

CODE
├─ Service layer separation
├─ Database transactions
├─ JSON casting for config
└─ Type hints everywhere

RECOMMENDED
├─ Redis caching (pricing rules)
├─ Queue jobs (notifications)
└─ Read replicas (reports)
```

---

## 🎨 API Response Format

### Success
```json
{
  "id": 1,
  "order_number": "ORD-2025-0001",
  "status": "CONFIRMED",
  "total": 450.00,
  "items": [...]
}
```

### Error
```json
{
  "error": "Customer credit limit exceeded"
}
```

### List (Paginated)
```json
{
  "data": [...],
  "current_page": 1,
  "per_page": 20,
  "total": 150
}
```

---

**Built with:** Laravel 11 • PHP 8.2 • MySQL 8 • Sanctum JWT

**Status:** Production-ready core API ✅
